export const CONFIG = {
  particleCount: 120,
  baseSpeed: 0.3,
  flowZoom: 0.005, // How "zoomed in" the noise field is
  timeScale: 0.0001, // How fast the flow field changes over time
  mouseInfluenceRadius: 300,
  mouseForce: 0.05,
  colors: [
    'rgba(45, 212, 191, ', // Teal 400
    'rgba(139, 92, 246, ', // Violet 500
    'rgba(56, 189, 248, ', // Sky 400
    'rgba(167, 139, 250, '  // Purple 400
  ]
};